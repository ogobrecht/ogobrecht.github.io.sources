{
  "name": "ogobrecht.github.io.sources",
  "private": true,
  "scripts": {
    "server": "hugo server --port 8889",
    "server-future": "hugo server --port 8889 --buildFuture",
    "build": "hugo --cleanDestinationDir"
  }
}
